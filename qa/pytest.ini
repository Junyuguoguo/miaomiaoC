[pytest]
testpaths = tests
addopts = -q
markers =
    api: API black-box tests against a running backend/site
    static: package structure/configuration tests
    ui: browser UI tests with Playwright
